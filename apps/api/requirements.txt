# Socle minimal — l'API démarre et sert tous les modules sans dépendance lourde.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27
duckdb>=0.10
shapely>=2.0
# scipy : triangulation de Delaunay pour les isochrones en alpha-shape.
# Sans lui, /api/routing/isochrone retombe sur l'enveloppe convexe.
scipy>=1.11

# Optionnels — voir requirements-full.txt pour tout activer
